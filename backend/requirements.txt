# @file requirements.txt
# @role
# Pins backend Python dependencies used by Django, DRF, MySQL, AI, and test tooling.

# Web framework - Core Django and REST API support
django>=6.0.5
djangorestframework>=3.17.1

# Environment - Read settings from environment variables
django-environ>=0.13.0

# Database - MySQL
mysqlclient>=2.2.8
cryptography>=42.0.0

# Database - Vector
pinecone >= 9.0.0

# API - CORS
django-cors-headers>=4.9.0

# API - Documentation
drf-spectacular>=0.28.0

# Test - Pytest runner and Django integration
pytest>=8.0.0
pytest-django>=4.9.0

# Olfit Specific Dependencies
requests>=2.32.3
scrapling[fetchers]>=0.4.8
numpy>=1.26.0
scikit-learn>=1.4.0
openai>=1.12.0
python-dotenv>=1.0.1
pillow>=10.2.0
sqlparse>=0.5.5
tzdata>=2026.2

# ----------------------------------------------------------------
# Update History
# 2026-05-18: git diff 기준 @file/@role header와 backend dependency manifest EOF footer 추가. (worker: @nobrain711)
# 2026-05-15: chore(backend): setup Pinecone infrastructure and update config. (author: @Gloveman)
# 2026-05-13: feat(perfumes): persist perfume image assets. (author: @nobrain711)
# 2026-05-11: feat(backend): migrate django fragrance apiAdds the Django REST backend, scent engine services, perfume data loa.... (author: @nobrain711)
# 2026-05-13: fix(docker): normalize startup script handling. (author: @nobrain711)
# ----------------------------------------------------------------

# EOF: requirements.txt
